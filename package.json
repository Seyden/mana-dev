{
  "name": "@mana-app/dev",
  "version": "0.1.8",
  "description": "Mana development toolchain — builds and serves .mana source packages using Rust + V8 + Go",
  "private": true,
  "scripts": {
    "bump": "node scripts/bump-version.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mana-app/emulator": "^0.0.1",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "rolldown": "^0.15.0"
  }
}
